# Screenshots

Captured from a real browser against the running app (`scripts/seed_demo.py` data).

- `editor.png` — the main editor: section bands, colour-coded cues, loop regions,
  energy/low/vocal curves, configuration panel, review list.
- `reasons.png` — a recommendation expanded to show the model's own weighted terms.
- `export.png` — the export dialog and lowering report.
